image: golang:latest

stages:
  - test
  - build
  - deploy

format:
  stage: test
  script:
    - go mod init hello
    - go mod tidy
    - go build -o main ./

compile:
  stage: build
  script:
    - ls 
  artifacts:
    paths:
      - main

deploy:
  stage: deploy
  script: echo "Define your deployment script!"
  environment: production
